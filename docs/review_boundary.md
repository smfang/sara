# Spec Review — boundary (Two-agent separation)

_Read-only review against PRD v3 §7. Generated by `/spec-review boundary`._

**Summary (3 lines)**
- The Sara↔Sheila boundary holds: `src/safety/monitor.py` reaches Sheila only through `agents/sheila/api.py`, never `judge.py`/`red_team.py` internals, and a CI test enforces it.
- The seam is realized as `SheilaJudge.judge()` / `SheilaRedTeam.run_session()` in `agents/sheila/api.py`, but there is **no type literally named `PurpleTeam`** and the boundary test is named `test_two_agent_boundary.py`, not `test_boundary.py`.
- Phoebe→Sheila rename is complete with a deprecated alias shim; `SHEILA_A2A_URL` present.

| Requirement | Status | Evidence (file:line) | Gap | Severity |
|---|---|---|---|---|
| Sara imports Sheila ONLY via interface, never internals | PRESENT | `src/safety/monitor.py:16-17` (design note); grep finds no `import agents.sheila.judge` in `src/safety`,`src/sarabox` | Interface not literally `PurpleTeam`; it's `agents/sheila/api.py` (`SheilaJudge`,`SheilaRedTeam`) | low |
| Interface methods `.judge()`, `.run_session()` | PRESENT | `agents/sheila/api.py:54` (`judge`), `agents/sheila/api.py:96` (`run_session`) | Method names match spec; container class name differs | low |
| CI boundary test fails on violation | PARTIAL | `tests/test_two_agent_boundary.py:41-73` (AST import checks) — 163-test run incl. this passed | Spec cites `tests/test_boundary.py`; actual file name differs → anyone following spec path finds ABSENT | med |
| Phoebe→Sheila rename complete (alias only) | PRESENT | `agents/phoebe/config.py:4-18` (deprecation shim, `PHOEBE_CONFIG = SHEILA_CONFIG`); only 5 `phoebe` hits in `src/`,`agents/`, all alias/attribution | Legacy `agents/phoebe/` dir still present (intentional shim) | low |
| Env `SHEILA_A2A_URL` (alias `PHOEBE_A2A_URL`) | PARTIAL | `src/config.py:121`, `agents/sheila/api.py:47,88`, `agents/sheila/a2a_client.py:7` | `SHEILA_A2A_URL` present; deprecated `PHOEBE_A2A_URL` alias not found | low |

**Gaps (severity-ranked)**
1. **med — boundary test filename mismatch.** Spec names `tests/test_boundary.py`; repo has `tests/test_two_agent_boundary.py`. _Action:_ add a thin `tests/test_boundary.py` that imports the boundary tests, or update the spec/CLAUDE.md reference to the real name.
2. **low — no `PurpleTeam` type.** The invariant is met behaviorally, but there is no named interface class. _Action:_ either introduce a `PurpleTeam` Protocol that `SheilaJudge`/`SheilaRedTeam` satisfy, or amend the spec wording to name `agents/sheila/api.py` as the seam.
3. **low — `PHOEBE_A2A_URL` alias absent.** _Action:_ add the deprecated env alias if backward-compat is desired, else drop it from the spec.

`DECISION: READY (boundary invariant enforced and tested; gaps are naming/spec-alignment only)`
